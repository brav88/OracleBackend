CREATE TABLE PWAP.persons2(
    person_id NUMBER GENERATED BY DEFAULT AS IDENTITY,
    first_name VARCHAR2(50) NOT NULL,
    last_name VARCHAR2(50) NOT NULL,
    PRIMARY KEY(person_id)
);

INSERT INTO PWAP.persons2 (first_name, last_name) VALUES ('John', 'Doe');

SELECT * FROM PWAP.persons2;

GRANT ALL ON PWAP.persons2 TO CWEB;


DECLARE
  FIRST_NAME VARCHAR2(200);
  LAST_NAME VARCHAR2(200);
BEGIN
  FIRST_NAME := 'Will';
  LAST_NAME := 'Travis';

  CWEB.PCKGPERSONS.INSERTPERSON(
    FIRST_NAME => FIRST_NAME,
    LAST_NAME => LAST_NAME
  );
--rollback; 
END;