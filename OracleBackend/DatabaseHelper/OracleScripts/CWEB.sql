CREATE TABLE CWEB.persons(
    person_id NUMBER GENERATED BY DEFAULT AS IDENTITY,
    first_name VARCHAR2(50) NOT NULL,
    last_name VARCHAR2(50) NOT NULL,
    PRIMARY KEY(person_id)
);

INSERT INTO CWEB.persons (first_name, last_name) VALUES ('John', 'Doe');
SELECT * FROM CWEB.persons;
SELECT * FROM PWAP.persons2;

DECLARE
  FIRST_NAME VARCHAR2(200);
  LAST_NAME VARCHAR2(200);
BEGIN
  FIRST_NAME := 'Will';
  LAST_NAME := 'Travis';

  PCKGPERSONS.INSERTPERSON(
    FIRST_NAME => FIRST_NAME,
    LAST_NAME => LAST_NAME
  );
--rollback; 
END;

DECLARE
  PERSONID NUMBER;
BEGIN
  PERSONID := 5;

  PCKGPERSONS.DELETEPERSON(
    PERSONID => PERSONID
  );
--rollback; 
END;

GRANT EXECUTE ON CWEB.PCKGPERSONS.INSERTPERSON TO PWAP;